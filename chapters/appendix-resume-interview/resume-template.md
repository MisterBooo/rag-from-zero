# RAG 项目经历 · 简历模板(填空版)

> 用法:把下面每个 `<...>` 占位替换成你自己的真实内容和**实测数字**。
> 数字一定要用第 10 章那套评估方法亲手量出来,别凭空编。

---

## RAG 问答系统开发与优化(<行业,如 保险/法律/医疗> 领域)

`<公司/项目名>` ｜ `<角色,如 算法工程师/实习>` ｜ `<起止时间>`

### 项目背景(2-3 句:规模 + 价值)
负责从 0 到 1 构建 `<行业>` 领域智能问答系统,处理 `<N>` 份多格式文档
(`<PDF/PPT/Word/扫描件...>`),服务 `<M>` 名 `<用户群体>` 的 `<业务场景>`,
日均处理 `<X>` 次查询,响应时间 `<Y>` s,答案准确率 `<Z>`%。

### 主要工作(每个模块 3-5 条,每条 = 问题 → 我的方案 → 量化效果)

**【解析与切分】**(对应教程第 3 章)
- 问题:`<现状 + 数据,如 开源工具表格识别率仅 a%>`
- 我的方案:`<具体技术,如 结构感知切分 / 表头复制 / 智能 overlap>`
- 效果:`<量化,如 截断率从 b% 降到 c%>`

**【检索召回】**(对应第 4、5 章)
- 我设计 `<向量检索 + BM25 混合检索>`,用 `<RRF 融合>`,
  把 `<Recall@5 / MRR 从 a 提到 b(你的实测)>`

**【重排】**(对应第 6 章)
- 我用 `<Cross-Encoder 精排>`,通过 `<批量推理 + 量化>`
  把延迟从 `<a ms 降到 b ms>`

**【多轮 / 记忆】**(对应第 8 章)
- 我实现 `<指代消解 + 滑动窗口 + 摘要压缩>`,
  支持 `<N>` 轮对话不超 token 上限

**【生成与溯源】**(对应第 9 章)
- 我做 `<仅依据资料的 prompt 约束 + 后处理引用归因 + 幻觉检测>`,
  把 `<幻觉率 / 用户信任度 等指标>` 改善到 `<...>`

### 项目成果(四个维度)
- 技术指标:`<Recall / MRR / 延迟,你的实测>`
- 答案质量:`<准确率 / 满意度>`
- 业务价值:`<服务多少人 / 降本多少 / 处理多少查询>`
- 技术沉淀:`<评估集 / 领域词典 / 可复用框架>`

### 技术栈
`<LLM、Embedding、Rerank、向量库、解析工具、框架...>`

---

> 自检:每一条 bullet 都满足「动词 + 具体技术方案 + 量化指标」了吗?
> 每一句你都能在面试里展开讲 20 分钟吗?能,才留下;不能,删掉或补课。
